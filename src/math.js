export function sum(a, b) {
  return a + b;
}

// var math = math || {}; // math 네임스페이스
// (function () {
//   function sum(a, b) {
//     return a + b;
//   }
//   math.sum = sum; // 네임스페이스 함수 등록
// })();
